{
	"version": 2,
	"outputDirectory": "dist",
	"builds": [
		{ "src": "robot-game-server/server.js", "use": "@vercel/node" },
		{ "src": "**", "use": "@vercel/static" }
	],
	"routes": [
		{ "src": "/api/(.*)", "dest": "robot-game-server/server.js" },
		{ "src": "/(.*)", "dest": "/dist/$1" }
	]
}
